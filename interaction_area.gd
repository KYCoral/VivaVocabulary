class_name InteractionArea
extends Area2D

@export var action_name: String = "interact"


func _ready():
	pass
#	interaction_area.interact = Callable (self._on_interact)pass


func _on_interact():
	pass
#	DialogManager.start_dialog(global_positon,lines,speech,sound)
#	sprite.flip_h = true if interaction_area.get_overlapping_bodies()[0].global_position.x < global_position.x else f 
#	await DialogManager.dialog_finished

